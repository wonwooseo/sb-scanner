services:
  - type: web
    name: sb-scanner
    image:
      url: docker.io/antares1959/sb-scanner-web:latest
    startCommand: ./api --port $PORT
    envVars:
      - key: LOGLEVEL
        value: INFO
    autoDeploy: false
